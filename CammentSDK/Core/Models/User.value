User includes(RMBuilder) excludes(RMCoding)  {
  NSString *userId
  NSString *cognitoUserId
  NSString *fbUserId
  NSString *username
  NSString *firstname
  NSString *lastname
  NSString *email
  NSString *visibility
  NSString *userPhoto
}